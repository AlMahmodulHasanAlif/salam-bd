# Loaded automatically by `vite build` (production mode).
# The Android app is built from `npm run build`, so it talks to the LIVE backend.
# Origin only — the app code appends `/api` itself (see src/hooks/useAxios.jsx).
VITE_API_URL=https://salambd.com
